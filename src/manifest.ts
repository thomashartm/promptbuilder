import { defineManifest } from 'wxt/manifest';

export default defineManifest({
  name: 'Prompt Builder',
  description: 'Craft prompts using frameworks',
  version: '1.0.0',
  permissions: ['storage'],
  icons: {
    48: 'icons/icon.png'
  }
});
